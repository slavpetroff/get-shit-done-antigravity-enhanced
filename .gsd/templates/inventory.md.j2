# GSD Inventory: {{ type }}

> **Status**: `ACTIVE`
> **Last Sync**: {{ last_sync }}
> **Total**: {{ count }}

---

{{ human_readable_content }}

---

<gsd_registry type="{{ type }}">
    <!-- MACHINE-READABLE REGISTRY -->
    {% for item in items %}
    <item id="{{ item.id }}" confidence="{{ item.confidence }}">
        <name>{{ item.name }}</name>
        <path>{{ item.path }}</path>
        <description>{{ item.description }}</description>
    </item>
    {% endfor %}
</gsd_registry>
